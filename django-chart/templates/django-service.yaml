apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-django
  labels:
    app: django
spec:
  selector:
    app: django
  ports:
    - name: http
      port: 8000
      targetPort: 8000
